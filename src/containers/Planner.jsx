const Planner = () => {

    return (
        <div style={ Styles.container}>
            <h1> Planner </h1>
        </div>
    )
}

export default Planner

const Styles = {
    container: {
        display: "flex",
        justifyContent: "center"
    }
}