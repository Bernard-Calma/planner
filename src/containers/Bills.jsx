const Bills = () => {
    return(
    <div style={Styles.container}>
        <h1>Bills</h1>
    </div>)
}

export default Bills

const Styles = {
    container: {
        display: "flex",
        justifyContent: "center"
    }
}